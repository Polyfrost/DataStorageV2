name: "Update Bundles"
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  UpdateBundles:
    name: Update packwiz sources to latest stable
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install node dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache mod jars
        uses: actions/cache@v4
        with:
          path: scripts/.cache/jars
          key: mod-jars-${{ hashFiles('data/oneclient/bundles/.mrpacks/**/*.pw.toml') }}
          restore-keys: |
            mod-jars-

      - name: Update bundles
        id: update
        env:
          PACKWIZ_TOKEN: ${{ secrets.PACKWIZ_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x ./scripts/*.sh
          ./scripts/update-bundles.sh

      # Gate: updates that break the dependency graph are reverted (only the mods
      # involved), so everything else still gets committed. Reverts and pre-existing
      # failures are annotated and fail the job at the end, after the push.
      - name: Check compatibility
        id: gate
        run: ./scripts/gate-bundle-updates.sh

      # Only the packwiz source (.mrpacks) is committed. Pushing to main triggers
      # the Deploy workflow, which regenerates the bundles and publishes to GitHub Pages.
      - name: Commit and push updated sources
        run: |
          git config --global user.name "Bundle Updater[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/oneclient/bundles/.mrpacks
          git commit -m "chore(update-bundles)" || echo "No changes to commit"
          for i in 1 2 3 4 5; do
            git pull --rebase --autostash && git push && break
            echo "Push failed (attempt $i), retrying..."
            sleep 5
          done

      - name: Fail if any update was held back
        if: steps.update.outputs.degraded == 'true' || steps.gate.outputs.degraded == 'true'
        run: |
          echo "::error::Some bundles were not updated; see the annotations from the compatibility check"
          exit 1
